{
    email {$EMAIL}
}

{$DOMAIN} {
    # Reverse proxy to frontend
    reverse_proxy frontend:80

    # API endpoints redirection
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Optional: Clerk Webhooks if needed
    handle /webhooks/* {
        reverse_proxy backend:8000
    }

    # Better compression
    encode gzip zstd
}
